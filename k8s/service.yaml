apiVersion: v1
kind: Service
metadata:
  name: expense-tracker-service

spec:
  type: NodePort
  selector:
    app: expense-tracker

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007